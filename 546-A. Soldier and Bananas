#include <bits/stdc++.h>
#include <math.h>
#include <string>
using namespace std;
int main()
{
    long long k,n,w,price=0,dollar,i;
    cin>>k>>n>>w;
    for(i=1; i<=w; i++)
    {
        price+=(i*k);
    }
    dollar=price-n;
    if(dollar<=0) cout << "0";
    else cout << dollar;
}
